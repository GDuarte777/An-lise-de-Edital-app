/**
 * Carimbo da versão publicada, injetado pelo Vite em tempo de build
 * (ver `define` em vite.config.ts). Declarado aqui, num arquivo sem imports
 * nem exports, para valer como global em todo o projeto.
 */
declare const __BUILD_ID__: string;
